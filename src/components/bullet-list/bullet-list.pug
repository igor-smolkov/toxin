//- 
  options.items

mixin bullet-list(options = {})
  - const blockName = "bullet-list"
  - const { items = [] } = options

  UL(class=blockName)
    each item, index in items
      LI(
        class=`${blockName}__item`
        class=(index===0) ? `${blockName}__item_first` :""
      )=item