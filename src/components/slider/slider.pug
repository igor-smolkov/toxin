//-
  options.values

mixin slider(options = {})
  - const blockName = "slider"
  DIV(
    class=blockName
    class=`js-${blockName}`
  )
    DIV(
      class=`js-${blockName}__root`
      data-values=options.values
    )