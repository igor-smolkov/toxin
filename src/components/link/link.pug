mixin link(options = {})
  - const blockName = "link";
  - const { isSelected, isHovered, link, text } = options;

  A(
    class=blockName,
    class=isSelected && `${blockName}_selected`,
    class=isHovered && `${blockName}_hovered`,
    href=link
    title=text
  )= text
