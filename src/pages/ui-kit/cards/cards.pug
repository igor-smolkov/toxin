extends ../../../layout

include ../../../components/logo/logo.pug
include ../../../components/card/card.pug

block variables
  - var title = 'TOXIN - UI kit'
  - var page = 'page_cards'

block content
  +logo
  .cards
    +card('wishes')
    +card('check-in')
    +card('room')
    +card('log-in')
    +card
    +card
    +card('room-rate')